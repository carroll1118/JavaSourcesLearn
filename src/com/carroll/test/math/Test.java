package com.carroll.test.math;

import java.util.Arrays;

public class Test {
    public static void main(String[] args) {
        Math.pow(2,3);
        Math.max(2,3);
        Arrays.asList();

        //Arrays.sort();
    }
}
